{
  "name": "immunodefense",
  "version": "1.0.0",
  "private": true,
  "description": "ImmunoDefense — tower defense inmunologico (canvas 2D, sin bundler)",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "serve": "node scripts/build-web.mjs && npx --yes serve www -l 5173",
    "sync": "npm run build && cap sync",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
